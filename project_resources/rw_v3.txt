<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Photography Portfolio</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { height: 100%; font-family: sans-serif; }
    body { display: flex; flex-direction: column; }

    /* header */
    header {
      position: relative;
      display: flex;
      align-items: center;
      padding: 1em;
      background: #ececcb;
    }
    /* center the inline nav buttons */
    .nav-inline {
      display: flex;
      gap: 1em;
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
    }
    .nav-inline a {
      text-decoration: none;
      color: #333;
    }

    /* burger toggle (hidden on wide) */
    .nav-toggle {
      display: none;
    }
    .nav-toggle summary {
      font-size: 1.5em;
      cursor: pointer;
      list-style: none;
    }

    /* dropdown nav styling */
    .nav-dropdown a {
      display: block;
      padding: 1em;
      border-bottom: 1px solid #ddd;
      text-decoration: none;
      color: #333;
      background: #fff;
    }

    /* blur content when menu open */
    body:has(.nav-toggle[open]) #main > article {
      filter: blur(5px);
      pointer-events: none;
    }

    /* mobile: hide inline, show burger & dropdown */
    @media (max-width: 600px) {
      .nav-inline { display: none; }
      .nav-toggle { display: block; }
      .nav-toggle nav { display: none; }
      .nav-toggle[open] nav {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        max-height: calc(100vh - 100%);
        overflow-y: auto;
      }
    }

    /* main & footer */
    #main { flex: 1; display: flex; }
    #main > article { flex: 1; padding: 1em; }
    footer { padding: 1em; background: #ececcb; }
  </style>
</head>
<body>
  <header>
    Photography by You
    <nav class="nav-inline">
      <a href="#">Photos</a>
      <a href="#">Gallery</a>
      <a href="#">Contact</a>
    </nav>
    <details class="nav-toggle">
      <summary>&#9776;</summary>
      <nav class="nav-dropdown">
        <a href="#">Photos</a>
        <a href="#">Gallery</a>
        <a href="#">Contact</a>
      </nav>
    </details>
  </header>

  <div id="main">
    <article>
      <!-- your page content here -->
      <p>This is where your main content goes.</p>
    </article>
  </div>

  <footer>Footer content</footer>
</body>
</html>
