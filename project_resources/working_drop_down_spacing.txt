<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Photography Portfolio</title>
  <style>
    /* reset & base */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { display: flex; flex-direction: column; min-height: 100vh; font-family: serif; }

    /* header: three equal zones */
    header {
      position: relative;
      display: flex;
      align-items: center;
      padding: 1em 2em;
      background: #ececcb;
    }
    .header-section { flex: 1; display: flex; align-items: center; }
    .header-section.title { justify-content: flex-start; }
    .header-section.nav   { justify-content: center; }
    .header-section.toggle{ justify-content: flex-end; }

    /* title */
    .site-title {
      all: unset;
      cursor: pointer;
      font-size: 2em;
      font-weight: 900;
      color: #333;
    }

    /* inline links */
    .nav-links { display: flex; gap: 2em; }
    .nav-links a { text-decoration: none; color: #333; font-size: 1.2em; }

    /* mobile toggle (hidden desktop) */
    .toggle-details { display: none; }
    .toggle-details summary {
      all: unset;
      display: block;
      cursor: pointer;
      font-size: 1.5em;
      padding: 0.25em;
    }

    /* blur when open */
    body:has(.toggle-details[open]) main article {
      filter: blur(5px);
      pointer-events: none;
    }

    /* mobile rules */
    @media (max-width: 600px) {
      .header-section.nav   { display: none; }
      .toggle-details       { display: flex; }
      .toggle-details nav   { display: none; }
      .toggle-details[open] nav {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background: #fafafa;
        max-height: calc(100vh - 100%);
        overflow-y: auto;
      }
      .toggle-details[open] nav a {
        padding: 1em;
        border-bottom: 1px solid #ddd;
        text-decoration: none;
        color: #333;
        font-size: 1.2em;
      }
    }

    /* main & footer */
    main { flex: 1; padding: 2em; }
    footer { padding: 2em; background: #ececcb; }
  </style>
</head>
<body>
  <header>
    <div class="header-section title">
      <button class="site-title">Photography by abc abcabcabca</button>
    </div>
    <div class="header-section nav">
      <nav class="nav-links">
        <a href="#">Photos</a>
        <a href="#">Gallery</a>
        <a href="#">Contact</a>
      </nav>
    </div>
    <div class="header-section toggle">
      <details class="toggle-details">
        <summary>&#9776;</summary>
        <nav>
          <a href="#">Photos</a>
          <a href="#">Gallery</a>
          <a href="#">Contact</a>
        </nav>
      </details>
    </div>
  </header>

  <main>
    <article>
      <!-- Your main content here -->
      <p>Resize to under 600 px, click the ☰, and you’ll see the dropdown on a light-grey background, with the article blurred behind it.</p>
    </article>
  </main>

  <footer>
    Footer content
  </footer>
</body>
</html>
